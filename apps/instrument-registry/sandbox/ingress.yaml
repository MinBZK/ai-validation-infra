apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ing
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    #TODO(Berry): use treafik basic auth middleware
spec:
  rules:
    - host: instrument-registry.apps.digilab.network
      http:
        paths:
          - backend:
              service:
                name: svc
                port:
                  number: 80
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - instrument-registry.apps.digilab.network
      secretName: instrument-registry-tls
