apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tn-ai-validation-keycloak

resources:
  - ../../base
  - secret-keycloak.yaml
  - secret-postgres.yaml
  - secret-postgres-super.yaml
